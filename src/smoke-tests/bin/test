#!/bin/bash

set -e
set -x

go install -v ./vendor/github.com/onsi/ginkgo

export GOPATH=$GOPATH:$(pwd)/../..
CF_COLOR=false CF_VERBOSE_OUTPUT=true ginkgo -r -v -noColor=true -keepGoing=true -trace=true -slowSpecThreshold=300 service adapter digest
